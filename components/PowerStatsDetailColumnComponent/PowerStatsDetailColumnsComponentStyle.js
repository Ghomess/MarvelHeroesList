import {StyleSheet} from 'react-native';

export const powerStatsDetailColumnsComponentStyles = StyleSheet.create({
  statsContainerSecond: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: '100%',
  },
});
